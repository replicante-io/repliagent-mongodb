# MongoDB agent configuration for local development.
addresses:
  cluster: "localhost:27017"

telemetry:
  logs:
    level: DEBUG
    mode: terminal

node_id: n-fake
version_detect:
  file: "dev-mongod-version.txt"
